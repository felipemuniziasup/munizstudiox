{
  "name": "muniz-studio-x-starter-v1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "http-server -p 5173 public",
    "preview:dist": "http-server -p 5174 dist",
    "logos": "node scripts/generate-logos.cjs",
    "optimize": "node scripts/optimize-images.cjs",
    "sitemap": "node scripts/gen-sitemap.cjs",
    "build": "node scripts/build.cjs",
    "cf:build": "npm run logos && npm run optimize && npm run sitemap && npm run build"
  },
  "devDependencies": {
    "csso": "^5.0.5",
    "fs-extra": "^11.3.1",
    "glob": "^11.0.0",
    "html-minifier-terser": "^7.2.0",
    "http-server": "^14.1.1",
    "sharp": "^0.34.3",
    "terser": "^5.31.0"
  }
}
